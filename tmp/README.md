# tmp/

Scratch space for stage 2 (`20_extract_euchromatin.sh`) alone --
intermediate `.paf` and `.bed` files from the liftover step, left in
place after a run for debugging rather than cleaned up automatically.
Not read by any other stage.

Not version-controlled.
