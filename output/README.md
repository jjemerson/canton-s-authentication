# output/

Container for pipeline deliverables, split into three subdirectories:
`figures/` (the publication figures), `tables/` (summary statistics),
and `reports/` (unused by this pipeline -- see its own README). See
each subdirectory's own README for specifics.

Not version-controlled (see `.gitignore`); entirely regenerated by
running the pipeline.
